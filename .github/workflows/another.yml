name: Another Workflow
on:
  push:
    branches:
      - master  # Change this to the branch you want to trigger the workflow on

jobs:
  deploy:
    uses: ${{ github.repository }}.github/workflows/base.yml@master  # Reference the reusable workflow dynamically
